"""
Application configuration for the dealership Django app.
"""
from django.apps import AppConfig


class DjangoappConfig(AppConfig):
    """
    Defines the AppConfig subclass used by Django to set up
    the dealership application within the project.
    """
    name = 'djangoapp'
